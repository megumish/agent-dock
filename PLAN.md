# PLAN

[VISION.md](./VISION.md) の理念を、[DESIGN.md](./DESIGN.md) の構成に沿って段階的に実現するための計画である。フェーズの並びは「意味契約 → 観測 → 射影と開示 → リプレイと審査 → 采配の shadow と助言 → 限定委任 → 評価代行の解放」— VISION の「始動」（ユーザーが全役割を兼任する状態から、証拠の蓄積が委任を広げる）を工程に写したものである。

## 進め方の原則

- **各フェーズはそれ自体で使える価値を出す**。記録だけでも「自分のモデル利用の実態が見える」価値がある。
- **決定はフェーズ入口で ADR 化する**。DESIGN の Decision Register の「決定境界」に従う。意味契約（D-02 封筒・D-13・D-14・D-15・D-16）は最初のデータ収集前（Phase 0）に確定し、数値・アルゴリズムは証拠が揃うまで決めない。
- **比較条件は結果を見る前に登録する**。評価契約（D-16）は Phase 0 の成果物であり、holdout は射影の学習に使う前に隔離する。
- **手間の上限をフェーズ出口条件に含める**。評価運用がユーザーの手間の上限を超えるなら、そのフェーズは完了ではない。
- **自律性の2軸を同じフェーズで広げない**。仕事の**采配の委任**（Phase 4–5）と価値判断の**評価の代行**（Phase 5b）は別軸として独立に解放する。異常時に routing 由来か teacher-signal 由来かを切り分けられることが目的。
- **出口条件は成果物の有無ではなく検証の証拠で書く**。各フェーズの出口には、検証証拠・stop/rollback 条件・確定した ADR・意図的に未決のまま残す Decision を列挙する。
- **自作は既定ではない**。各フェーズの ADR では、既存の OSS・既存ツールで足りるかを先に検討する（DESIGN の Decision Register の原則に従う）。

## フェーズ間の主要依存

- フィードバック収集（D-06）は Phase 1 から。
- 評価契約・baseline（D-16）は Phase 0 で確定（データより先）。
- 外部シグナルの自動化はリプレイが動く Phase 3 以降（トリガの受け皿が先）。
- 資格の**執行**（D-09）は Phase 5。それまで資格レジストリは「候補」のみで権限を持たない。
- 評価代行の教師信号化は Phase 5b。それまで代行評価は shadow（計測のみ）。

## Phase 0 — 意味契約と歩く骨格

**目的**: 最初のデータを集める前に、後から直せない意味を確定し、最小の縦切りを通す。

**入口で確定する ADR（意味契約）**:
- 対象タスクコホート（何の仕事を計測対象にするか）と baseline・holdout の方式（D-16）
- ユーザー手間の上限（D-16 の一部・ユーザーが設定）
- 実行プロファイルと task/run/attempt の identity・因果リンク（D-13, D-14）
- 最小イベント封筒（D-02 の契約部分）
- 送信・保持境界と単発許可の形（D-15）

**成果物**:
- `docs/adr/` と ADR テンプレート
- イベントスキーマ v0 と記録の最小 CLI
- **歩く骨格**: dummy または低リスクの実アダプタ1本で、command → run → event → 監査ビュー の縦切りを通す
- 開発環境整備（mise によるツール管理）

**出口条件**:
- 検証証拠: 縦切りの実行1件がイベントとして記録され、監査ビューから来歴がたどれる
- 確定 ADR: 上記の意味契約群
- 意図的未決: ストレージ最終形・射影式・減衰値・采配アルゴリズム（Decision Register に open のまま列挙）
- stop/rollback: スキーマの致命的欠陥はイベントが少ない今だけ作り直しを許す（以後は互換読み込みで対応）

## Phase 1 — 挙動を変えない観測

**目的**: 日常の仕事のやり方を変えずに、記録だけが増える状態を作る（采配はユーザー＝始動状態）。

**入口で確定する ADR**: D-18（アダプタ契約）→ D-01（実行基盤）、D-06（フィードバック UX）、D-20（dotfiles との責務分担 — 入口コマンドの置き場所）。

**成果物**:
- 入口コマンド／スキル（既存のエージェント起動をラップ）
- 実行アダプタ（導入済み CLI の headless 起動・実測の正規化・失敗/リトライ/部分成功の区別）
- 実測の自動記録（時間・実支出・帰結参照・ユーザー手間の代理指標）
- フィードバック・訂正・削除の経路（明示は1アクション以内、受動優先、削除は非機微来歴つき）

**出口条件**:
- 検証証拠: 対象コホートの主要経路が記録されている・失敗も記録されている・訂正/削除が来歴つきで機能した実績・手間が上限内
- stop/rollback: 記録の手間が上限を超えたら記録項目を削って続行（記録停止より簡略化を優先）

## Phase 2 — 射影と開示

**目的**: スコアが読める・比較の材料が貯まり始める。

**入口で確定する ADR**: D-03（taxonomy）、D-17 の最小形（不確実性・適用範囲の表現）、D-19（進化の機構）。

**成果物**:
- 射影エンジン v0（値＋不確実性＋適用範囲＋鮮度、版管理と rebuild 可能）
- 監査面（スコアカード: すべての値から根拠へ一段、采配理由・方針・上書きの閲覧、ユーザー異議＝訂正イベント）
- ベースライン台帳の運用開始（事前登録済み比較の集計、holdout の隔離を機械的に担保）

**出口条件**:
- 検証証拠: 事前登録した比較の集計が出せる・holdout が射影の入力に混じっていないことを確認できる・異議申立てが訂正イベントとして反映された実績
- stop/rollback: 射影が誤解を招く場合（不確実性の過小表示）は表示を保守側に倒す

## Phase 3 — リプレイと審査（shadow）

**目的**: 新プロファイルの審査と評価の較正が、権限を伴わずに回る。

**入口で確定する ADR**: D-07（リプレイ）、D-08（外部取り込み3系統）、D-12 の初期形（監査サンプリング）、D-13 の等価性規則（暫定）。

**成果物**:
- リプレイハーネス（分離実行・n回サンプル・受け入れ済み成果との比較）
- 初期審査パイプライン（新プロファイル → リプレイ＋prior → **資格候補**の付与。権限なし）
- 代行評価の **shadow 運転**（評価は記録するが教師信号にしない。ユーザー評価との一致率を計測）
- 外部取り込み（価格の半自動更新・prior とシグナルの手動投入 → 再審査コーディネータへの接続）
- ドリフト定点観測の試行

**出口条件**:
- 検証証拠: 新プロファイル追加→審査→候補資格が一連で回った実績・代行の一致率が領域別に計測されている・シグナル→再審査の経路が1回以上動いた
- stop/rollback: リプレイ費が予算枠超過なら題材集合を縮小
- 意図的未決: 代行の教師信号化（5b まで封印）・資格の執行（5 まで封印）

## Phase 4 — 采配の shadow と助言

**目的**: 采配の質を、まず見せずに計測し、次に助言として提示する。

**入口で確定する ADR**: D-05（rank/abstain）、D-11 の前半（リスクゲート）。

**段階**:
- **4a shadow routing**: 推薦を計算するがユーザーに見せず、実選択と並べて記録する（推薦と実選択の一致・帰結の差を計測）
- **4b advisory**: 根拠＋abstain つきで提案し、毎回明示承認。推薦・提示・採択/棄却をすべて決定イベントとして分離記録

**出口条件**:
- 検証証拠: 事前登録した保留/将来データで、推薦に従った場合の品質・失敗率の非劣性と総コスト（手間込み）の改善見込み・attention 上限内・abstain が適切に発動している
- stop/rollback: 採択率または品質が基準未満なら 4a に戻す（提示をやめて計測を続ける）

## Phase 5 — 限定委任（采配）

**目的**: 資格・リスク・権限の独立ハードゲートを通る範囲で、低リスク・可逆な仕事から自動委任する。

**入口で確定する ADR**: D-04（役割の確定）、D-09（資格執行）、D-10（鮮度・再審査）、D-11 の後半（探索予算執行）。

**成果物**:
- 資格執行（起動経路の一本化＋ホスト機構の補助）— 資格・リスク・権限は**独立のゲート**として通す
- 探索予算の執行と消化開示
- 運転制御の検証: kill/rollback・タイムアウト・資格の期限切れ・エスカレーションが実際に機能すること
- 鮮度減衰・ドリフト再審査の定常運転（継続運用レーンへの引き渡し）

**出口条件**:
- 検証証拠: VISION 成功基準の初回実証（事前登録した比較で、非劣性＋総コスト改善）・kill/rollback とエスカレーションの実働確認・手間上限内
- stop/rollback: エスカレーション率が下がらない役割は助言モード（4b）に戻す。委任範囲は役割ごとに独立に広げ、一括で広げない

## Phase 5b — 評価代行の解放（采配とは別軸）

**目的**: shadow で実績を積んだ評価代行を、限定領域で教師信号として使い始める。

**解放の条件（すべて満たすまで開始しない）**:
- 十分な user-labeled 実績と、領域限定の一致率の**将来データでの**検証
- 審判と被評価者の独立性の担保
- 必須二重評価（監査サンプリング）が運転されている
- 一致率が閾値を下回ったときの自動停止・資格剥奪の機構

**出口条件**:
- 検証証拠: 代行込み運用で監査が持続し、代行由来の評価がイベント上で区別できる（routing 由来との切り分けが可能）
- stop/rollback: ずれの検知で該当領域の代行を自動停止し、ユーザー評価に戻す

## 継続運用レーン（全フェーズ横断）

フェーズの完了で終わらない定常の責務。各フェーズの出口条件には、そのフェーズまでに始まったレーン項目が「回り続けていること」を含める。

- 鮮度の失効と再審査（Phase 3 から）
- 外部シグナル → リプレイの接続（Phase 3 から）
- 資格の失効・剥奪（Phase 5 から）
- インシデント対応（自動化の停止と原因の切り分け — routing / teacher-signal の軸別）
- スキーマ・プロファイルの移行（D-19。Phase 2 から）
- 保持境界と削除の執行（Phase 0 から）
- 負担監視（ユーザー手間の計測と上限アラート — Phase 1 から）

## 運用リズム（候補・未決）

定常運転後の運用は未決だが、候補として: スコアカードの週次確認、予算と比較条件の月次見直し、再審査はトリガ駆動。いずれも手間の上限内に収まる形で Phase 5 までに決める。

## この計画の非スコープ

VISION の非目標（汎用ベンチマーク化・マルチユーザー化・完全自動運転・ベンダー固定）に加え、この計画では次を扱わない:

- ドック自身のクラウド常駐化（制御面はローカルのまま）
- 評価データの外部共有機構
- Phase 5 到達前の、資格に基づく無確認自動実行
- Phase 5b 到達前の、代行評価の教師信号化

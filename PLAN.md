# PLAN

[VISION.md](./VISION.md) の理念を、[DESIGN.md](./DESIGN.md) の構成に沿って段階的に実現するための計画である。フェーズの並びは「観測・記録 → 射影と開示 → リプレイと審査 → 助言的采配 → 資格付き委任」— VISION の「始動」（ユーザーが全役割を兼任する状態から、証拠の蓄積が委任を広げる）を工程に写したものである。

## 進め方の原則

- **各フェーズはそれ自体で使える価値を出す**。記録だけでも「自分のモデル利用の実態が見える」価値がある。次のフェーズの前提を作るためだけのフェーズは置かない。
- **決定はフェーズ入口で ADR 化する**。DESIGN の Decision Register に「決定時期」として割り当てられた D-xx を、そのフェーズの最初の作業として確定する。それまで選択肢は開いたままにする。
- **比較条件は結果を見る前に登録する**（VISION 成功基準）。ベースライン台帳は Phase 2 で作るが、Phase 1 の時点から記録は比較に使える形で残す。
- **手間の上限をフェーズ出口条件に含める**。どのフェーズでも、評価運用がユーザーの手間の上限を超えるならそのフェーズは完了ではない。
- **フェーズは重なってよいが、飛ばさない**。委任の拡大（Phase 4→5）は、その根拠となる証拠の蓄積（Phase 1〜3）なしに前倒ししない。

## Phase 0 — 足場

**目的**: 評価イベントの記録が始まる最小の土台を作る。

**成果物**:
- docs 構成と ADR テンプレート（`docs/adr/`）
- イベントスキーマ v0（実行プロファイル・タグ・来歴・schema_version を含む最小形）
- 記録の最小 CLI（手動でもイベントを1件書ける）
- リポジトリの開発環境整備（mise によるツール管理）

**入口で確定する決定**: D-02（イベント記録・ストレージ）。

**出口条件**: 実タスク1件が評価イベントとして記録されている。

**リスクと手当**: スキーマの過剰設計 → v0 は最小に留め、schema_version と「イベントが真実・射影は再計算可能」の原則を逃げ道にする。

## Phase 1 — 観測

**目的**: 日常の仕事がドック経由で記録される状態を作る。采配はまだユーザーが行う（始動状態）。

**成果物**:
- 入口コマンド／エージェントスキル（既存のエージェント起動をラップし、記録を自動化）
- 実行アダプタ v0（導入済みエージェント CLI の headless 起動と実測の正規化）
- 実測の自動記録（所要時間・実支出・帰結の参照）
- フィードバック最小 UX（明示は1アクション以内、受動シグナル優先）

**入口で確定する決定**: D-01（実行基盤）、D-06（フィードバック UX）。

**出口条件**: 主要な作業経路がドック経由で記録され、記録・フィードバックの手間が上限内に収まっている。

**リスクと手当**: 記録の手間が採用を妨げる → 受動記録を優先し、明示フィードバックは監査対象に絞る。

## Phase 2 — 射影と開示

**目的**: スコアが読める・比較が始まる。

**成果物**:
- 射影エンジン v0（値＋不確実性＋適用範囲＋鮮度）
- スコアカード CLI（すべての値に根拠イベントへの参照を添える監査ビュー）
- ベースライン台帳（比較条件の事前登録と、保留/将来データでの比較レポート）

**入口で確定する決定**: D-03（射影とタスク分類）。

**出口条件**: 事前登録した比較の集計が出せる。スコアカードのすべての値から根拠に一段でたどれる。

**リスクと手当**: 選択バイアスの誤読（采配が選んだ仕事だけがデータになる）→ 射影は最初から不確実性・適用範囲つきで表示し、点数の一人歩きを防ぐ。

## Phase 3 — リプレイと審査

**目的**: 新しい実行プロファイルの初期審査と、評価の較正が回る。

**成果物**:
- リプレイハーネス（分離環境での再演・n 回サンプル・受け入れ済み成果との比較）
- 代行評価器＋監査サンプラ（一致率による資格、常時監査）
- 外部取り込み（価格の半自動更新、ベンチ prior と外部シグナルの手動投入）
- 初期審査パイプライン（新プロファイル追加 → リプレイ＋prior → スコアカード反映）

**入口で確定する決定**: D-07（リプレイ方式）、D-08（外部取り込み）、D-12（代行監査）。

**出口条件**: 新しいモデル／プロファイルを追加してから、初期審査を経てスコアカードに載るまでが一連で回る。

**リスクと手当**: リプレイのコスト超過 → 題材集合を小さく保ち、予算枠を先に決めてから回す。

## Phase 4 — 助言的采配

**目的**: ドックが采配を提案し、ユーザーが確認して実行する。

**成果物**:
- 采配エンジン（助言モード）: 資格・射影・序列に基づく提案と、その理由の提示
- 裁定ガード（序列の機械化とエスカレーションの UX）
- リスク判定ルール v0（低リスク判定のチェックリスト）
- 提案の採択／棄却も決定イベントとして記録（采配役の教師データになる）

**入口で確定する決定**: D-05（采配方式）、D-11 の前半（リスク判定）。

**出口条件**: 提案の採択率と、採択した場合の受け入れ品質・コストの非劣性が、保留データで確認できる。

**リスクと手当**: 提案品質が低く信頼を失う → 提案には必ず根拠を併記し、外れた提案も決定イベントとして次の較正に使う。

## Phase 5 — 資格付き委任

**目的**: 資格の範囲で自動委任し、探索・鮮度管理を定常運転に載せる。

**成果物**:
- 資格執行（起動経路の一本化＋ホスト機構の補助）
- 探索予算の執行と消化状況の開示
- 鮮度減衰・ドリフト検知・再審査の定常運転（定点観測の定期実行）
- 役割ごとの委任範囲の段階的拡大

**入口で確定する決定**: D-04（役割と適性要件の確定版）、D-09（資格執行）、D-10（鮮度・再審査）、D-11 の後半（探索予算）。

**出口条件**: 事前登録した比較で「受け入れ品質・失敗率の非劣性を保った総コストの初回改善」を実証する（VISION の成功基準）。評価運用の手間が上限内。

**リスクと手当**: 自動化の前倒し → 委任は役割ごとに独立に広げ、エスカレーション率が下がらない役割は助言モードに留める。

## 運用リズム（候補・未決）

定常運転後の運用は未決だが、候補として: スコアカードの週次確認、予算と比較条件の月次見直し、再審査はトリガ駆動（定期＋外部シグナル）。いずれも手間の上限内に収まる形で Phase 5 までに決める。

## この計画の非スコープ

VISION の非目標（汎用ベンチマーク化・マルチユーザー化・完全自動運転・ベンダー固定）に加え、この計画では次を扱わない:

- ドック自身のクラウド常駐化（制御面はローカルのまま）
- 評価データの外部共有機構
- Phase 5 到達前の、資格に基づく無確認自動実行

# PLAN

[VISION.md](./VISION.md) の理念を、[DESIGN.md](./DESIGN.md) の構成に沿って段階的に実現するための計画である。フェーズの並びは「意味契約 → 観測 → 射影と開示 → リプレイと審査 → 采配の shadow と助言 → 限定委任 → 評価代行の解放」— VISION の「始動」を工程に写したものである。

## 進め方の原則

- **各フェーズはそれ自体で使える価値を出す**。記録だけでも「自分のモデル利用の実態が見える」価値がある。
- **決定はフェーズ入口で ADR 化する**。DESIGN の Decision Register の決定境界に従う。意味契約（D-02 封筒・D-13 identity・D-14・D-15・D-16・D-19 原則・D-21）は最初のデータ収集・最初の非 dummy 実行の前に確定し、数値・アルゴリズムは証拠が揃うまで決めない。
- **比較条件は結果を見る前に登録する**。事前登録は版と window を持ち、変更は新しい登録として扱う。
- **手間の上限をフェーズ出口条件に含める**。
- **自律性の2軸を同じフェーズで広げない**。采配の委任（Phase 4–5）と評価の代行（Phase 5b）は独立に解放し、異常時に routing 由来か teacher-signal 由来かを切り分けられるようにする。
- **採用か自作かは ADR で選ぶ**。既存 OSS・既存ツールと自作の両方を選択肢に含める（どちらも既定としない）。

## 出口条件の共通契約

各フェーズの出口は、成果物の有無ではなく次の5点で判定する:

1. **検証証拠**: 何が確認されたか（成果物として保存され、事前登録の版・window を参照する）
2. **stop / rollback 条件**: 何が起きたら止めるか・巻き戻すか。**rollback とはモード・資格・導出状態の巻き戻しであり、イベントの削除ではない**
3. **確定した ADR** の列挙
4. **意図的に未決のまま残す Decision** の列挙
5. **稼働中の継続レーンの health 証拠**（そのフェーズまでに始まったレーンが回り続けていること）

## フェーズ間の主要依存

- フィードバック収集（D-06）は Phase 1 から。価格 contract v0（D-08）は**最初の real run の前**。
- 評価契約・baseline（D-16）は Phase 0（データより先）。
- Phase 0 で real アダプタを使うなら D-18 minimal contract が先。用意できなければ Phase 0 は dummy のみ。
- 外部シグナルの自動化はリプレイが動く Phase 3 以降。
- 資格の執行（D-09）は Phase 5。それまで資格は candidate のみで権限を持たない。
- 代行評価の教師信号化は Phase 5b。それまで shadow（計測のみ）。
- 探索予算（D-11）は探索の開始前に確定。

## Phase 0 — 意味契約と歩く骨格

**目的**: 最初のデータを集める前に、後から直せない意味と境界を確定し、最小の縦切りを通す。

**入口で確定する ADR**: 対象タスクコホートと baseline・holdout 方式・手間上限（D-16）／プロファイル identity と紐付け（D-13 identity, D-14）／最小イベント封筒＋**可逆な v0 ストア**（D-02。最終ストアは意図的未決）／送信・保持境界・単発許可・方針スナップショット（D-15）／**最小 fail-closed ガード**（D-21）／互換性・移行の原則（D-19 原則）。

**成果物**:
- `docs/adr/` と ADR テンプレート
- イベントスキーマ v0・記録の最小 CLI・**replay manifest の保存開始**（task 入力参照・リポジトリ revision・ツール/環境/プロファイルのレシート・方針/プロトコル版・因果参照）
- **歩く骨格**: dummy または（D-18 minimal contract を先に確定した場合のみ）低リスクの実アダプタ1本で、command → guard → run → event → 監査ビュー の縦切り
- 開発環境整備（mise によるツール管理）

**出口**:
- 検証証拠: 縦切り1件の記録と来歴の追跡／最小ガードの fail-closed 動作／**bypass negative test**（ガードを迂回できない）／run 非依存イベント（方針変更）が封筒で表現できる
- stop/rollback: スキーマの作り直しは **disposable なテスト名前空間のみ**で許す（正典記録を消さない）
- 確定 ADR: 上記の意味契約群
- 意図的未決: 最終ストア・射影式・減衰値・采配アルゴリズム・taxonomy
- レーン: 保持・削除（開始）

## Phase 1 — 挙動を変えない観測

**目的**: 日常の仕事のやり方を変えずに、記録だけが増える状態（manual 経路・始動状態）。

**入口で確定する ADR**: D-18（full contract）→ D-01（実行基盤）、D-06（フィードバック UX）、D-08 の価格 contract v0、D-20（dotfiles 境界）、D-24（状態機械と in-doubt 表現）。

**成果物**:
- 入口コマンド／スキル（manual 経路のラップ。ルータ不要）
- 実行アダプタ（headless 起動・実測正規化・planned/started/completed/failed/unknown の区別・レシート）
- 実測の自動記録（時間・実支出（価格 contract v0 による）・帰結参照・attention 代理指標）
- フィードバック・訂正・削除の経路（受動優先・削除は非機微来歴つき）
- **wrapper の観測影響の計測**（ラップが timing・context・プロファイルを変えていないか）

**出口**:
- 検証証拠: 対象コホートの主要経路の記録率／失敗・リトライ・in-doubt の記録実例／訂正・削除の成功実例／**再構成可能な task が最低1件**（manifest から）／attention が上限内
- stop/rollback: **必須の意味・安全フィールドが手間制約に収められない場合は、フィールドを削るのではなく停止して設計を見直す**。記録の手間超過は簡略化で対応
- 確定 ADR: 上記
- 意図的未決: taxonomy・射影・采配
- レーン: 負担監視・インシデント（開始）・価格鮮度（開始）

## Phase 2 — 射影と開示

**目的**: スコアが読める・比較の材料が貯まり始める。

**入口で確定する ADR**: D-03（taxonomy）、D-17 最小形（不確実性・適用範囲）、D-10 の freshness v0、D-19 機構、D-23（監査再構成契約）。

**成果物**:
- 射影エンジン v0（値＋不確実性＋適用範囲＋鮮度・版管理と rebuild）
- 監査面 v0（値→根拠の一段追跡・采配理由・方針の版・上書き記録・異議＝訂正イベント）
- ベースライン台帳の運用開始（事前登録済み比較の集計。**holdout は gate 別の frozen cohort または future window とし、アクセス統制・opened=spent・変更は新登録**）

**出口**:
- 検証証拠: 事前登録した比較の集計（版参照つき）／holdout が射影の入力に混じっていない機械的確認／D-23 受入テスト（過去の決定の再構成）／rebuild と「削除→無効化→再計算が削除を尊重」の実演
- stop/rollback: 不確実性の過小表示が判明したら表示を保守側に倒す
- 意図的未決: 較正方式・減衰の数値
- レーン: schema/profile migration（開始）

## Phase 3 — リプレイと審査（shadow）

**目的**: 新プロファイルの審査と評価の較正が、権限を伴わずに回る。

**入口で確定する ADR**: D-07（リプレイと VCS アダプタ）、D-08（prior・シグナル）、D-10 の scheduler v0、D-12 の暫定高率サンプリング、D-13 の等価性（暫定）、D-04 の暫定役割（candidate/shadow 用）、D-22 の candidate 運用。

**成果物**:
- リプレイハーネス（manifest からの再構成・分離実行・n 回サンプル・受け入れ済み成果との比較）
- 初期審査パイプライン（新プロファイル → リプレイ＋prior → **candidate 資格**。権限なし）
- 代行評価の shadow 運転（教師信号にしない・一致率計測のみ・高率監査）
- 外部取り込み（prior・シグナルの手動投入 → 再審査コーディネータ接続）・ドリフト定点観測の試行

**出口**:
- 検証証拠: 審査一連の実績／**隔離の検証**（外部副作用なし・複数回再現・prior/シグナル非混入の negative test）／代行の領域別一致率／シグナル→再審査の実働1回以上
- stop/rollback: **隔離破りは corpus 縮小ではなく即停止**。予算超過で corpus を変える場合は新しい事前登録＋開示
- 意図的未決: 代行の教師信号化（5b まで封印）・資格の執行（5 まで封印）・routing 選定
- レーン: 鮮度・再審査、signal→replay（開始）

## Phase 4 — 采配の shadow と助言

**目的**: 采配の質を、まず見せずに計測し、次に助言として提示する。**4a→4b は独立のゲートで区切る。**

**入口で確定する ADR**: 4a 入口 = D-05 の実験用暫定方針・D-11 のリスク分類・D-17 の較正。**4a 出口／4b 入口 = routing choice ADR**（4a の証拠に基づく）。

**段階**:
- **4a shadow routing**: 推薦を計算するが提示しない。実選択と並記録。ゲート: coverage・較正・abstention の適切さ・**無影響**（shadow が挙動を変えていない）を確認してから 4b へ
- **4b advisory**: 根拠＋abstain つき提案・毎回明示承認。推薦・提示・採択/棄却を分離記録

**検証設計**: shadow の並記録だけでは「従った場合」の反実仮想は識別できない。4b では**事前登録した識別可能な前向き設計**（paired replay・低リスク領域での無作為化・switchback 等）で、採択・不採択双方の品質・コスト・attention を検証する。**関連（association）しか得られない場合は因果の主張をせず、その根拠だけで自動委任に進まない。**

**出口**:
- 検証証拠: 事前登録した保留/将来データでの非劣性・総コスト・attention（版参照つき）／abstain の適切な発動／4a の無影響確認
- stop/rollback: 採択率または品質が基準未満なら 4a に戻す（モードの巻き戻し）
- 意図的未決: 探索予算の数値（探索開始前に確定）
- レーン: 継続

## Phase 5 — 限定委任（采配）

**目的**: 資格・リスク・権限の独立ハードゲートを通る範囲で、低リスク・可逆な仕事から自動委任する。

**入口で確定する ADR**: D-04（最終役割）、D-09（資格執行）、D-10（較正・定常化）、D-11（探索予算）、D-22（完全形）、D-24（回復手順の完全形）。

**手順**: live 開始の前に **fault injection と negative test**（資格・リスク・権限ゲートの独立性／non-bypass・fail-closed／kill・予算・期限切れの動作）を通し、その後**役割を絞った canary** から広げる。

**即時 stop 条件**: 境界違反／イベント欠落・重複実行／kill 失敗／予算超過／非劣性の破れ／期限切れ資格の使用。

**出口**:
- 検証証拠（**別々に証拠化する**): (1) routing の成功 = 事前登録した比較での品質・失敗率の非劣性＋総コストの初回改善 (2) evaluation の成功 = 自前射影が external-prior-only baseline を予測で上回る (3) attention が持続的に上限内 (4) ユーザーの最終判定（成功基準の判定はユーザー）
- stop/rollback: エスカレーション率が下がらない役割は 4b に戻す。委任範囲は役割ごとに独立に広げる
- レーン: 資格失効（開始）

## Phase 5b — 評価代行の解放（采配とは別軸）

**目的**: shadow で実績を積んだ評価代行を、限定領域で教師信号として使い始める。

**入口で確定する ADR**: D-12 の最終形（shadow 証拠に基づくサンプリング・独立性・不一致処理）。

**解放の条件（すべて満たすまで開始しない）**: 十分な user-labeled 実績と領域限定の一致率の**将来データでの**検証／審判と被評価者の独立性／必須二重評価の運転／ずれ時の自動停止・剥奪機構。

**出口**:
- 検証証拠: **user label always wins の運用テスト**（ユーザー判断到着時に代行判定が正しく劣後する実例）／代行由来評価のイベント上の区別（routing 由来との切り分け）／label 品質の非劣性＋attention の削減
- stop/rollback: 独立性または監査の欠落を検知したら該当領域の代行を自動停止し、ユーザー評価に戻す

## 継続運用レーン（全フェーズ横断）

フェーズ完了で終わらない定常責務。**各レーンに owner（論理責務）・開始フェーズ・health 証拠・stop 条件を定める。**

| レーン | owner | 開始 | health 証拠 | stop 条件 |
|---|---|---|---|---|
| 保持・削除の執行 | 保持・削除執行 | Phase 0 | 削除要求の処理実績・rebuild conformance | 削除の取りこぼし検知で新規記録を停止し復旧 |
| 負担監視 | 総コスト・手間計測 | Phase 1 | attention 指標の継続計測と上限比 | 上限超過でフェーズ進行を停止 |
| インシデント対応 | 裁定ガード＋監査面 | Phase 1（最初の実運転から） | 軸別（routing / teacher-signal）の切り分け記録 | 切り分け不能なら該当自動化を全停止 |
| 価格の鮮度 | 外部取り込み | Phase 1 | 価格の最終更新時刻 | **更新不能時はコスト比較を unknown 扱い（fail-closed）** |
| schema / profile 移行 | イベントログ＋射影 | Phase 2 | rebuild の再現成功 | 互換読込の失敗で移行停止 |
| 鮮度・再審査 | 再審査コーディネータ | Phase 3 | トリガ→再審査の追跡記録 | 再審査の滞留で新規委任を停止 |
| signal→replay | 外部取り込み＋再審査 | Phase 3 | シグナル処理の実績 | — |
| 資格失効 | 資格レジストリ | Phase 5 | 期限・失効の執行記録 | 期限切れ資格の使用検知で委任停止 |

## 運用リズム（候補・未決）

定常運転後の運用は未決だが、候補として: スコアカードの週次確認、予算と**将来の**比較条件の月次見直し（**過去の holdout・登録は書き換えない**）、再審査はトリガ駆動。手間の上限内に収まる形で Phase 5 までに決める。

## この計画の非スコープ

VISION の非目標に加え、この計画では次を扱わない:

- ドック自身のクラウド常駐化（制御面はローカルのまま）
- 評価データの外部共有機構
- Phase 5 到達前の、資格に基づく無確認自動実行
- Phase 5b 到達前の、代行評価の教師信号化

# Agent Dock 実装計画

本計画は、[VISION.md](./VISION.md) の理念を [DESIGN.md](./DESIGN.md) の論理構成に沿って段階的に実現する。
フェーズは、意味契約、観測、射影と開示、リプレイと審査、采配のシャドー運転と助言、限定委任、評価代行の順に進む。
この順序は、VISION の「資格のない初期状態」から証拠に基づいて委任を広げる過程を、実装工程に対応させたものである。

## 本文で使う用語

- **ダミー実行**：ダミーアダプタだけを使い、実際の作業対象へ作用しない試行を指す。
- **実環境での実行**：導入済みの実行基盤を介して実際の作業を行う試行を指す。
- `manual`、`shadow`、`advisory`、`delegated` は、DESIGN が定める運用モードの列挙値である。
- `candidate` は、資格による権限をまだ持たない候補状態の列挙値である。
- **評価代行の観測状態**：資格前の候補による評価出力を記録するが、その出力自体を現在の判定、射影、資格判定の入力に使わない状態を指す。
  同じ対象へのユーザー判断と観測出力を比較して得た一致実績は、資格審査の証拠にできる。
  この状態は較正のための出力状態であり、評価を代行する権限ではない。
- **受動シグナル**：成果の残存、手戻り、採用などから得る観測または代理信号であり、教師信号ではない。
- **歩く骨格**：ダミーアダプタを用いて、入口から監査面までを端から端まで通す最小構成を指す。

## 進め方の原則

- 各フェーズは、その時点で利用できる成果を出す。
- 設計上の決定は、各フェーズの入口または設計判断一覧に明記した時点で Architecture Decision Record（ADR）として採択する。
- D-02 の封筒、D-13 の同一性、D-14、D-15、D-16、D-19 の互換原則、D-21 は Phase 0 で確定し、数値やアルゴリズムは証拠が揃うまで決めない。
- Phase 0 ではダミー実行だけを行う。
- **初回実行ゲート**：最初の実環境での実行より前に満たす移行条件を指す。
- 初回実行ゲートでは、D-03 の暫定語彙、D-08 の価格入力契約 v0、D-18 と D-24 の意味要件への適合、D-21 の適用を確認する。
- 初回実行ゲートを開く前に、コスト入力の鮮度、イベント完全性、インシデント対応を監視するレーンを稼働させる。
- D-18 の実行アダプタ要件と D-24 のイベント完全性要件は、それぞれ DESIGN の対応項目で定義し、その実現機構は ADR で選ぶ。
- 比較条件は、結果を見る前に登録する。
- 事前登録は版と評価期間を持ち、評価対象群の定義は登録時に凍結する。
- 後日のタグ訂正で過去の評価対象群や保留データの所属を書き換えず、分類変更を使う比較は新たに登録する。
- 手間の上限を各フェーズの出口条件に含める。
- 采配の委任と評価の代行は自律性の別軸として扱い、同じフェーズで両方を広げない。
- 採用と自作のどちらを選ぶかは ADR で決める。

## 出口条件の共通契約

各フェーズの出口は、次の五項目で判定する。
該当する内容がない項目には、「なし」とその理由を記録する。

1. **検証証拠**：確認した事実を成果物として保存し、該当する事前登録の版と評価期間を参照する。
2. **停止条件と巻き戻し条件**：モード、資格、導出状態をどの条件で巻き戻すかを記録する。
   巻き戻しはイベントの削除を意味しない。
3. **確定した ADR**：そのフェーズで採択した ADR を列挙する。
4. **意図的な未決事項**：設計判断一覧で決定期限が後のフェーズにある項目、または期限がない未決項目を列挙する。
   サブ決定も対象に含める。
   出口記録は、その時点のリポジトリのリビジョンを固定して参照する。
   本文には代表例だけを記載し、参照先リビジョンの設計判断一覧を正本とする。
5. **レーンの健全性**：そのフェーズまでに始まった継続レーンの証拠を、出口記録から固定して参照する。
   保存場所は、証拠管理を定める ADR に従う。

ADR の採択とフェーズ遷移の最終決裁はユーザーが行う。
エージェントは起草と提案を行えるが、ADR の採択や自律権限を広げる遷移を自動では行わない。

## フェーズ間の主要な依存関係

- フィードバック収集（D-06）は Phase 1 から始める。
- 価格入力契約 v0（D-08）と D-18、D-24 の最小契約は、Phase 1 の初回実行ゲートまでに確定する。
- 評価契約とベースライン（D-16）は、データを集める前の Phase 0 で確定する。
- 外部シグナルの自動取り込みは、リプレイが動く Phase 3 以降に始める。
- 資格の執行（D-09）は Phase 5 から始め、それまでは `candidate` に権限を与えない。
- 代行評価は Phase 5b まで評価代行の観測状態で計測する。
- 探索予算と介入予算（D-11）は、それぞれの支出を始める前に確定する。

## Phase 0 の意味契約と歩く骨格

### Phase 0 の目的

最初のデータを集める前に、後から変更すると既存記録の意味が変わる契約と境界を確定する。
ダミー実行で歩く骨格を通し、実環境での実行は行わない。

### Phase 0 の入口で確定する ADR

- D-16 で、評価対象群、ベースライン、保留データの作り方、手間の上限、評価対象群の凍結方法を確定する。
- D-13 と D-14 で、実行プロファイルの同一性と作業への紐付けを確定する。
- D-02 で、最小イベント封筒と可逆な v0 ストアを確定する。
- D-02 の最終ストアは、再評価条件を定めたうえで未決のまま残す。
- D-15 で、規範の表現と権威、単発許可、方針スナップショットを確定する。
- D-21 で、秘密、送信、保持の境界を執行する契約を確定する。
- D-19 で、互換性と移行の原則を確定する。

### Phase 0 の成果物

- `docs/adr/` と、ユーザーを承認者とする ADR テンプレートを用意する。
- イベントスキーマ v0 と記録用の最小 CLI を用意する。
- **再演マニフェスト**：タスク入力への参照、リポジトリのリビジョン、レシート、方針とプロトコルの版、因果参照を記録する成果物を指す。
- Phase 0 から再演マニフェストを保存し始める。
  VCS 管理下にないタスクでは、リポジトリのリビジョンを「該当なし」と記録する。
- ダミーアダプタを用いて、入口コマンド、裁定ガード、実行、イベント、監査面を順に通す歩く骨格を作る。
- プロジェクト内で再現できるツールチェーンを整備する。
- ツール管理方式は本文書で選定せず、現在の外部制約である mise の運用に従う。

### Phase 0 の出口条件

- 検証証拠として、歩く骨格を一件通した記録と、その来歴を追跡した結果を保存する。
- 最小ガードが、必要な情報を取得できない場合に実行を拒否することを確認する。
- 否定試験で、ガードの迂回、未許可の外部送信、秘密の送信と記録を拒否できることを確認する。
- 否定試験で、単発許可の適用範囲、有効期限、再利用拒否が機能することを確認する。
- ダミー記録で保持期限を判定し、削除対象と参照および複製を列挙して、認可済み削除を実行する。
- D-21 の完了条件に従い、管理範囲内で追跡済みの正典記録、参照、複製から対象を再読込できないことを確認する。
- 管理範囲外または未追跡の複製を検出した場合は、削除完了とせずインシデントとして記録する。
- ユーザー方針が消去を求める場合は、参照、ハッシュ、削除済みを示す印も再読込できないことを確認する。
- 方針変更のように実行から独立したイベントも、最小封筒で表現できることを確認する。
- 歩く骨格の操作と ADR 運用に要した手間を計測し、上限との比を保存する。
- Phase 0 全体の手間が D-16 の上限内に収まることを確認する。
- スキーマを作り直せる範囲は、破棄可能なテスト用名前空間に限定する。
- 正典記録は削除せず、ユーザーが要求し、認可された削除だけを DESIGN の優先規則に従って実行する。
- 確定 ADR は、D-02 の封筒と v0 ストア、D-13 の同一性、D-14、D-15、D-16、D-19 の原則、D-21 とする。
- 意図的な未決事項の代表例は、D-02 の最終ストア、射影式、減衰値、采配アルゴリズム、タスク分類とする。
- 保持と削除のレーンを開始し、記録と消去の契約を検証した証拠を健全性の判定に使う。

## Phase 1 で始める実環境の観測

### Phase 1 の目的

`manual` モードの采配を変えずに、実環境での実行を自動で記録する。
裁定ガードとラッパーは実行のタイミングや入力文脈へ影響し得るため、実行全体の挙動不変は主張しない。
最初の実環境での実行は、初回実行ゲートを通過してから行う。

### Phase 1 の初回実行ゲート

- D-03 で、明示的な未分類状態を含む暫定語彙と自由タグの扱いを確定する。
- D-18 と D-24 の共同最小契約が、DESIGN の両項目に定めた意味要件を満たすことを確認する。
- 意味要件を満たす実現機構は ADR で選ぶ。
- 共同最小契約を確定してから、D-01 の実行基盤を選ぶ。
- D-06 でフィードバックの操作方法と、受動シグナルの適合率および明示入力の応答率の分母、判定不能条件、見直し条件を確定する。
- D-08 で価格入力契約 v0 を確定する。
- D-20 で本リポジトリと dotfiles の責務境界を確定する。
- D-21 の境界執行契約が、実環境での実行にも適用されることを確認する。
- 非冪等または不可逆な操作は、実行前の重複防止を保証できない場合に拒否することを確認する。
- コスト入力の鮮度、イベント完全性、インシデント対応の各レーンを稼働させる。

### Phase 1 の成果物

- `manual` モードを包む入口コマンドまたはスキルを用意する。
- `manual` モードでは采配ルータを使わない。
- 実行アダプタは、ヘッドレス起動、実測値の正規化、実行状態の区別、レシートの保存を担う。
- 実行状態の語彙は D-24 の ADR で確定する。
- 各評価イベントへ D-03 の暫定語彙によるタグを付け、分類できない場合は未分類状態を明示する。
- D-16 の契約に従い、所要時間、実支出の内訳、換算できないローカル計算資源の使用量、換算規則の版、帰結への参照、手間の代理指標を記録する。
- 受動シグナルを観測または代理信号として自動記録し、明示フィードバックは監査対象と必要な場面へ絞る。
- 受動シグナルを教師信号として扱わず、現在のユーザー判定を上書きしない。
- 訂正と削除の経路を用意する。
- 削除の非機微な来歴は、安全であり方針が許す場合だけ DESIGN の優先規則に従って残す。
- 遅延フィードバックと並行実行を含む制御したケースを作り、D-14 の因果参照を検証する。
- 実行プロファイルの構成変更が生じた場合は、変更前後を別の識別子で記録し、D-13 の較正用ペアとして保存する。
- ラッパーが実行のタイミング、入力文脈、実行プロファイルへ与える影響を計測する。
- 意図的なクラッシュ、イベントの重複配送、実行指示の重複を発生させ、D-24 の検出と突き合わせを試験する。
- 非冪等または不可逆な操作では、重複した実行指示を作用前に拒否できることを試験する。

### Phase 1 の出口条件

- 評価対象群の主要経路について、記録率を保存する。
- D-03 の暫定語彙について、未分類率、複数ラベルの被覆率、タグの安定性を保存する。
- 三つの分類指標を算出できない場合は、正式な初期体系を判定不能とし、暫定語彙または計測方法を見直す。
- D-17 の射影候補ごとに、利用可能なイベント数、欠測率、タスクタグと実行プロファイルの分布を保存する。
- データ量または偏りによって適用範囲を定められない射影候補は、Phase 2 の射影カタログ v0 に採用しない。
- 失敗、再試行、結果未確定の状態を記録した実例を保存する。
- D-14 の制御ケースで、遅延フィードバックの誤接続がなく、共同成果を共同のまま保持し、曖昧な参照を保留または拒否できることを確認する。
- 訂正と削除に成功した実例を保存する。
- 受動シグナルの適合率と明示入力の応答率を、D-06 で定めた分母に従って保存する。
- いずれかの指標が判定不能または見直し条件に該当する場合は、用途を広げずにフィードバックの操作設計を見直す。
- 再演マニフェストから少なくとも一件のタスクを再構成できることを確認する。
- 手間が事前に定めた上限内に収まることを確認する。
- ラッパーの観測影響を計測した結果を保存する。
- D-20 で選んだ配置について、更新回数、同期回数、同期不整合、同期に要した手間を保存する。
- 同期不整合を検出した場合または手間が D-16 の上限を超えた場合は、D-20 の責務分担を見直す。
- クラッシュ試験でイベントの欠落を検出できることを確認する。
- 非冪等または不可逆な操作の重複実行を防止できない場合に、安全側で拒否した記録を保存する。
- 必須の意味項目または安全項目を手間の上限内で記録できない場合は、実環境での実行を停止して設計を見直す。
- 任意項目または入力操作だけが手間を超過させる場合は、必須項目を削らずにその部分を簡略化する。
- 確定 ADR は、D-03 の暫定語彙、D-18 の最小形と完全形、D-24 の最小形、D-01、D-06、D-08 の価格入力契約 v0、D-20 とする。
- 意図的な未決事項の代表例は、タスク分類、射影、采配、リスク分類、D-12、D-02 の最終ストアとする。
- 保持と削除、総コストと手間、イベント完全性、インシデント対応、コスト入力の鮮度を継続レーンとして監視する。

## Phase 2 の射影と開示

### Phase 2 の目的

射影の値から根拠までを追跡できる表示を提供する。
事前登録した比較に使うデータの蓄積と集計を始める。

### Phase 2 の入口で確定する ADR

- D-03 でタスク分類を確定する。
- D-17 の最小形で、射影カタログ v0 と各軸の意味、入力イベントを確定する。
- D-10 で鮮度の最小形を確定する。
- D-19 でスキーマと射影を移行する機構を確定する。
- D-23 で監査再構成契約を確定する。

### Phase 2 の成果物

- 射影エンジン v0 は、射影カタログ v0 の各軸を実データから計算する。
- 各射影には、値、不確実性、適用範囲、鮮度、計算に使った版を付ける。
- 射影エンジンは、保存済みイベントから射影を再構築できるようにする。
- 監査面 v0 は、値から根拠までを一段で追跡できるようにする。
- 監査面 v0 は、采配理由、適用した方針の版、上書き記録、訂正イベントを表示する。
- 削除済みの情報は、取得できない理由を添えて表示する。
- ベースライン台帳の運用を始める。
- 保留データには、ゲートごとに凍結した評価対象群または将来の評価期間を使い、アクセスを統制する。
- 一度開いた保留データは消費済みとして再利用せず、条件を変える場合は新たに登録する。

### Phase 2 の出口条件

- 事前登録の版を参照し、比較結果を集計する。
- 射影カタログ v0 の各軸を実データから計算して表示した実例を保存する。
- D-13 の変更前後ペアを得た場合は、別の識別子を発行し、変更前の証拠を変更後へ自動的に可搬しなかった記録を保存する。
- 変更前後ペアを得られない場合は、証拠を可搬しない暫定規則を採択し、較正できていないことを開示する。
- 保留データを射影の形成または采配方式の調整に使っていないことを機械的に確認する。
- 削除済みの情報を含むケースで、D-23 の受け入れ試験を行う。
- 射影の再構築が削除を尊重することを実演する。
- 不確実性を過小に表示していた場合は、その射影を判定に使うことを停止する。
- 表示を再開する前に、不確実性の範囲または判定不能であることを明示できるように修正する。
- Phase 2 全体の手間が D-16 の上限内に収まることを確認する。
- 確定 ADR は、D-03、D-17 の最小形、D-10 の鮮度 v0、D-19 の機構、D-23 とする。
- 意図的な未決事項の代表例は、較正方式、減衰値、采配方式、D-12、再評価中の最終ストアとする。
- 既存レーンに加えて、スキーマと実行プロファイルの移行を監視するレーンを開始する。

## Phase 3 のリプレイ審査

### Phase 3 の目的

新しい実行プロファイルを、権限を与えずに審査する。
観測出力自体を射影や資格判定の入力に使わず、同じ対象へのユーザー評価と比較して得た一致実績だけを資格審査の証拠にする。

### Phase 3 の入口で確定する ADR

- D-07 で、リプレイ方式、VCS アダプタ、判定基準、再演予算を確定する。
- D-08 で、外部事前情報と外部シグナルの取り込み方を確定する。
- D-10 で、鮮度に基づく再審査スケジュール v0 を確定する。
- D-12 で、代行評価を高い割合で監査する暫定方式を確定する。
- D-13 で、実行プロファイルの等価性と証拠の可搬性を暫定的に確定する。
- D-04 で暫定的な役割を確定する。
- D-22 で `candidate` の運用方法を確定する。
- D-02 の最終ストアを定期的に再評価する。

### Phase 3 の成果物

- リプレイハーネスは、再演マニフェストから実行を再構成し、作業対象から分離した環境で複数回実行する。
- 判定には、D-07 で確定した判定基準を使う。
- 受け入れ済み成果との差分は診断にだけ使い、判定基準にはしない。
- 判定基準を用意できない題材は審査対象から外す。
- 受け入れ済み成果を審判や被験プロファイルへ見せない。
- リプレイの実支出、ローカル計算資源の使用量、反復回数、ユーザーの手間を、D-07 の再演予算に対して記録する。
- 初期審査パイプラインは、新しい実行プロファイルをリプレイと外部事前情報で審査し、権限のない `candidate` として登録する。
- 代行評価は資格前の較正出力である観測状態で運転し、同じ対象へのユーザー評価と比較して領域別の一致率を計測する。
- 外部事前情報と外部シグナルを手動で取り込み、再審査コーディネータへ接続する。
- ドリフトを定点観測する手順を試行する。

### Phase 3 の出口条件

- 一連の審査を完了した実績を保存する。
- DESIGN のリプレイハーネス責務が定める作業対象への副作用を発生させず、複数回再現できることを確認する。
- 否定試験で、外部事前情報と外部シグナルが一次証拠へ混入しないことを確認する。
- 代行評価の領域別一致率を計測し、D-12 の ADR で定めた最小標本数を満たす。
- 外部シグナルを受けて再審査を一件以上実行する。
- 外部事前情報と外部シグナルの手動取り込みに要した手間を別々に保存する。
- リプレイが D-07 の再演予算内で完了したことを確認する。
- リプレイ、手動取り込み、評価監査を含む Phase 3 全体の手間が D-16 の上限内に収まることを確認する。
- 作業対象との隔離が破れた場合は、リプレイを直ちに停止する。
- 題材を減らしても隔離機構の欠陥は解消しないため、隔離を修復して再検証するまで再開しない。
- 予算超過を理由に題材集合を変更する場合は、比較条件を新たに事前登録して変更を開示する。
- 確定 ADR は次のとおりである。
  - D-07。
  - D-08 の外部事前情報と外部シグナル。
  - D-10 の再審査スケジュール v0。
  - D-12 の暫定方式。
  - D-13 の暫定等価性と可搬性。
  - D-04 の暫定役割。
  - D-22 の `candidate` 運用。
- 意図的な未決事項の代表例は、代行評価の限定利用、資格の執行、采配方式、探索予算、D-02 の最終ストアとする。
- 代行評価の限定利用は Phase 5b まで、資格の執行は Phase 5 まで開始しない。
- 既存レーンに加えて、鮮度と再審査、外部シグナルからリプレイまでの処理、代行評価の継続監査を開始する。

## Phase 4 の采配助言

### Phase 4 の目的

采配の推薦を `shadow` で計測してから、`advisory` でユーザーへ提示する。
Phase 4a から Phase 4b への移行は、独立したゲートで判定する。

### Phase 4a のシャドー運転

- Phase 4a の入口で、D-05 の実験用暫定方針、D-11 のリスク分類 v0、D-17 の較正方式を確定する。
- D-05 の実験用暫定方針には、被覆率、較正誤差、推薦を控える判断について、Phase 4b へ進むための指標と閾値を定める。
- D-11 では、過剰エスカレーションとリスクルールの誤判定の定義、分母、判定不能条件、執行用の分類を見直す条件を定める。
- 通常の推薦と、証拠の薄い候補を試す探索推薦を計算して記録する。
- D-03 の正式な初期体系について、タグ別の候補被覆率と判断保留率を記録する。
- 推薦はユーザーへ提示せず、実行にも使わない。
- Phase 4a では探索予算を支出しないため、探索予算をまだ確定しなくてよい。
- 推薦を提示も実行もしなかったことを確認し、この確認を「無影響」の判定とする。
- `shadow` の観測だけでは推薦による帰結差を識別できないため、因果効果を主張しない。

### Phase 4b の助言運転

- Phase 4a の出口で、事前登録した指標が閾値を満たしたことを確認する。
- Phase 4b の入口で、Phase 4a の証拠に基づいて D-05 の采配方式を確定する。
- Phase 4b の入口で、助言として行う探索の予算と低リスクの適用範囲を確定する。
- 無作為化または期間を切り替える比較を使う場合は、介入予算も Phase 4b の開始前に確定する。
- 根拠と、推薦を控える選択肢を添えて提案し、実行ごとにユーザーの明示承認を得る。
- 推薦の計算、ユーザーへの提示、採択または棄却、実行を別々のイベントとして記録する。
- 予算内かつ低リスクの範囲では、ユーザーが承認した探索提案を実行する。

### Phase 4b の検証設計

- 採択した提案と採択しなかった提案の品質、総コスト、手間を、事前登録した前向き設計で比較する。
- D-03 のタグを使う采配と帰結差の関連を、同じ前向き設計で検証する。
- 比較設計には、対にしたリプレイ、低リスク領域での無作為化、期間ごとに采配方式を切り替える設計を使用できる。
- 比較に使う設計、評価対象群、評価期間、指標、閾値は、結果を見る前に登録する。
- 得られた証拠が関連だけを示す場合は、因果効果を主張しない。
- 関連だけを示す証拠を根拠に、自動委任へ進まない。

### Phase 4 の出口条件

- 事前登録の版を参照し、保留データまたは将来データに対する品質の非劣性、総コスト、手間を検証する。
- Phase 4 全体の手間が D-16 の上限内に収まることを確認する。
- タグ別の候補被覆率、判断保留率、采配と帰結差の関連を保存する。
- 推薦を控える判断が、事前登録した条件で発動したことを確認する。
- 過剰エスカレーション率とリスクルールの誤判定率を、D-11 で定めた分母に従って保存する。
- いずれかのリスク指標が判定不能または見直し条件に該当する場合は、執行用のリスク分類を確定せず、D-11 の分類を見直す。
- Phase 4a で推薦を提示も実行もしなかったことを確認する。
- Phase 4a から Phase 4b へのゲートを通過した記録を保存する。
- Phase 4a で探索推薦を記録した実例と、Phase 4b でユーザーが承認した探索を実行した実例を保存する。
- 採択率または品質が事前登録した閾値を満たさない場合は、`advisory` から `shadow` へ戻す。
- `shadow` へ戻した場合は、D-05 の采配方式を再検討する。
- 確定 ADR は、D-05 の実験用暫定方針と采配方式、D-11 のリスク分類 v0 と助言探索予算、D-17 の較正方式とする。
- 意図的な未決事項の代表例は、委任探索の予算と適用範囲、執行用のリスク分類、資格執行、D-12 の最終形、D-02 の最終ストアとする。
- 委任探索の予算と適用範囲は Phase 5 の入口までに確定する。
- Phase 3 までに始めたすべてのレーンについて、健全性の証拠を継続して保存する。

## Phase 5 の限定委任

### Phase 5 の目的

資格、リスク、権限をそれぞれ独立したゲートで確認し、低リスクで取り消し可能な仕事から自動委任する。

### Phase 5 の入口で確定する ADR

- D-04 で最終的な役割を確定する。
- D-09 で資格ゲートの執行方式を確定する。
- D-10 で鮮度と再審査を定常運用へ移し、D-22 の失効遷移と整合させる。
- D-11 で、Phase 4 の誤判定に関する証拠を使って執行用のリスク分類を確定する。
- D-11 で、委任探索に使う予算と適用範囲を、助言探索とは別の版として確定または再承認する。
- D-22 で、D-10 の定常形と整合した資格ライフサイクルの完全形を確定する。
- D-24 で障害から回復する手順の完全形を確定する。

### Phase 5 の開始手順

- `delegated` の本番運転を始める前に、障害注入試験と否定試験を行う。
- 障害注入試験では、同時実行による予算競合、重複した実行指示、クラッシュ後の回復と突き合わせ、高負荷時の停止を試す。
- 否定試験では、資格、リスク、権限の各ゲートを独立して検証する。
- 否定試験では、ゲートの迂回、必要情報がない場合の拒否、緊急停止、予算超過、資格の期限切れを検証する。
- 試験を通過した後、役割を限定したカナリア運転から始める。
- カナリア運転の最大適用範囲、評価期間、範囲を広げる条件は、開始前に登録する。
- 委任探索は、通常の采配委任とは別のゲートで判定する。
- 委任探索も、予算内かつ低リスクの範囲に限定したカナリア運転を経て解放する。

### Phase 5 の即時停止条件

- 境界違反を検出した場合は、該当する委任を直ちに停止する。
- イベントの欠落を検出した場合は、新しい受付と実行を直ちに停止する。
- 未解決の重複実行を検出した場合は、該当する委任を直ちに停止する。
- 緊急停止の失敗、予算超過、品質の非劣性の破れ、期限切れ資格の使用を検出した場合は、該当する委任を直ちに停止する。

### Phase 5 の出口条件

- 采配の成功は、事前登録した比較で品質と失敗率の非劣性を満たし、手間を含む総コストが初回改善したことで判定する。
- 評価の成功は、事前登録した予測指標で自前の射影が外部事前情報だけのベースラインを上回り、事前登録した指標で不適切な采配を減らしたことで判定する。
- 手間が継続して上限内に収まることを確認する。
- ユーザーが委任範囲を広げてよいと最終判定した記録を保存する。
- 委任探索が独立したゲートを通過し、予算内で動作した実例を保存する。
- エスカレーション率が事前登録した閾値を満たさない役割は、`advisory` へ戻す。
- 委任範囲は役割ごとに判定し、独立して広げる。
- 確定 ADR は、D-04 の最終形、D-09、D-10 の定常形、D-11 の執行用分類と予算、D-22 の完全形、D-24 の完全形とする。
- 意図的な未決事項の代表例は、Phase 5b の入口までに決める D-12 の最終形と、再評価条件つきで期限を設けない D-02 の最終ストアとする。
- D-02 の v0 ストアに限界が見つからない限り、最終ストアの選定を強制しない。
- 既存レーンに加えて、資格失効と成功基準の継続監視を開始する。

## Phase 5b の評価代行

### Phase 5b の目的

観測状態で実績を積んだ評価代行を、限定した領域でユーザー判断に劣後する別種の評価イベントとして使い始める。
評価代行の解放は、采配の委任とは別軸で判定する。

### Phase 5b の入口で確定する ADR

- D-12 の最終形で、独立性を評価する次元、領域ごとの最低監査率と最小標本数、信頼区間つき停止閾値、再開条件を確定する。

### Phase 5b の解放条件

- ユーザーが付けた評価の必要件数と、領域別一致率の閾値を D-12 で定める。
- 将来データで、必要件数と一致率の閾値を満たしたことを確認する。
- D-12 で定めた各次元について、評価代行の独立性を確認する。
- D-12 で抽出した監査サンプルは、ユーザー評価と代行評価による二重評価を必須とする。
- 各領域の監査率をゼロにしない。
- 一致率の低下または独立性の欠落を検出した場合に、評価代行を自動停止して資格を取り消せることを確認する。
- すべての条件を満たすまで、代行評価を限定領域の判定に使わない。

### Phase 5b の出口条件

- ユーザーが付けた評価を常に優先することを運用試験で確認する。
- 代行評価から生じたイベントと、采配から生じたイベントを区別して記録する。
- 事前登録した基準で、代行評価の品質がユーザー評価に対して非劣性を満たすことを確認する。
- 評価に要するユーザーの手間が、事前登録した基準より減ったことを確認する。
- Phase 5b 全体の手間が D-16 の上限内に収まることを確認する。
- 独立性または監査の欠落を検出した場合は、該当領域の評価代行を自動停止してユーザー評価へ戻す。
- D-12 の再開条件を満たすまで、停止した領域の評価代行を再開しない。
- 確定 ADR は D-12 の最終形とする。
- Phase 5b までが決定期限の構造的な設計判断は、この出口までにすべて確定する。
- 再評価条件つきで期限を設けていない D-02 の最終ストアは、意図的な未決事項として残せる。
- 以後に新しい構造的な設計判断が必要になった場合は、新しい D 番号で設計判断一覧に登録する。
- 代行評価の継続監査を含む全レーンについて、健全性の証拠を継続して保存する。

## 継続運用レーン（全フェーズ横断）

フェーズの完了後も、各レーンの定常責務を継続する。
各レーンには、担当コンポーネント、開始フェーズ、健全性の証拠、停止条件を定める。

<!-- markdownlint-disable MD013 -->

| レーン | 担当コンポーネント | 開始 | 健全性の証拠 | 停止条件 |
| --- | --- | --- | --- | --- |
| 保持と削除の執行 | 保持と削除の執行 | Phase 0 | 削除と秘匿の契約検証、派生射影の無効化、削除後の再構築適合 | 削除または秘匿の取りこぼしを検出したら、対象情報を使う射影と実行を停止 |
| 総コストと手間 | 総コストと手間の計測 | Phase 1 | 実支出の内訳、ローカル計算資源の使用量、配賦と換算規則の版、手間の指標と上限比 | 必須入力が欠けたらコスト比較を判定不能とし、上限を超えたらフェーズ進行を停止 |
| イベント完全性 | 実行アダプタとイベントログ | Phase 1 | 実行意図、レシート、欠落と重複の検出、定期的な突き合わせ | 記録の欠落を検出したら新しい受付と実行を停止し、未解決の重複実行を検出したら該当する委任を停止 |
| インシデント対応 | 裁定ガードと監査面 | Phase 1 | 采配と評価代行を切り分けた記録 | 原因を切り分けられなければ該当する自動化を停止 |
| コスト入力の鮮度 | 外部取り込みと総コストと手間の計測 | Phase 1 | API 価格の更新時刻、定額配賦とローカル資源換算規則の版 | 入力を更新できなければコスト比較を判定不能とし、その比較に基づく進行を停止 |
| スキーマと実行プロファイルの移行 | イベントログと射影 | Phase 2 | 再構築の再現成功 | 互換読み込みに失敗したら移行を停止 |
| 鮮度と再審査 | 再審査コーディネータ | Phase 3 | 再審査の契機から実行までの追跡記録 | 再審査の滞留で新規委任を停止 |
| 外部シグナルからリプレイまでの処理 | 外部取り込みと再審査 | Phase 3 | シグナル処理の実績と滞留がないこと | シグナル処理が滞留したら自動化を停止して手動確認へ移す |
| 評価代行の継続監査 | 監査サンプラー | Phase 3 | 領域別の最小標本数、一致率の鮮度、独立性の維持 | 監査欠落または一致率の低下を検出したら該当領域の代行を停止 |
| 資格失効 | 資格レジストリ | Phase 5 | 期限と失効の執行記録 | 期限切れ資格の使用検知で委任停止 |
| 成功基準の継続監視 | ベースライン台帳 | Phase 5 | 初回改善後の品質、失敗率、総コスト、手間、外部事前情報だけのベースラインに対する予測性能差、不適切な采配率 | 非劣性の破れまたは評価性能の劣化を検出したら、該当する `delegated` モードを `advisory` または `manual` へ戻す |

<!-- markdownlint-enable MD013 -->

## Phase 5 までに決める定常運用

定常運転後の確認周期は未決であり、次の案を候補とする。

- スコアカードを週次で確認する。
- 予算と将来の比較条件を月次で見直す。
- 過去の保留データ、事前登録、評価対象群の定義は書き換えない。
- 鮮度切れ、ドリフト、外部シグナルを契機に再審査する。
- 構成変更は新しい実行プロファイルとして登録し、資格を持たない状態から初期審査する。

確認周期は、手間の上限内に収まる形で Phase 5 までに決める。

## この計画の対象外

VISION の非目標に加え、この計画では次を扱わない。

- ドック自身のクラウド常駐化（制御面はローカルのまま）
- 評価データの外部共有機構
- Phase 5 到達前の、資格に基づく無確認自動実行
- Phase 5b 到達前の、代行評価を限定領域の判定に使う運用
